# Server Configuration
PORT=5000

# MongoDB Connection
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/rfp-management

# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/rfp-management

# Gmail SMTP Configuration (for sending emails)
# Use the same Gmail account for both SMTP and IMAP
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-16-char-app-password

# Gmail IMAP Configuration (for receiving emails)
# Use the same App Password as SMTP_PASS
IMAP_USER=your-email@gmail.com
IMAP_PASS=your-16-char-app-password
IMAP_HOST=imap.gmail.com
IMAP_PORT=993

# OpenRouter AI Configuration
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# AI Models (optional - defaults shown)
AI_MODEL=openai/gpt-4o
AI_MODEL_MINI=openai/gpt-4o-mini

# ============================================
# SETUP INSTRUCTIONS:
# ============================================
#
# 1. Gmail Setup:
#    - Enable 2-Factor Authentication on your Gmail account
#    - Generate App Password: https://myaccount.google.com/apppasswords
#    - Enable IMAP: Gmail Settings → Forwarding and POP/IMAP → Enable IMAP
#    - Use the SAME Gmail account and App Password for both SMTP and IMAP
#
# 2. MongoDB Setup:
#    Option A - Local: Install MongoDB Community Server and start mongod
#    Option B - Cloud: Create free cluster at https://www.mongodb.com/cloud/atlas
#
# 3. OpenRouter Setup:
#    - Sign up at https://openrouter.ai/
#    - Create API key at https://openrouter.ai/keys
#    - Add credits (minimum $5 recommended for testing)
#
# 4. Copy this file:
#    cp .env.example .env
#    Then fill in your actual values
#
# ============================================
